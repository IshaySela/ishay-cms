{
  "name": "cms-server",
  "version": "1.0.0",
  "description": "The express server for the svelte app",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p ./tsconfig.json",
    "dev": "node ./dist/server.js",
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.17",
    "@types/mongodb": "^4.0.7",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongodb": "^5.0.1"
  }
}
